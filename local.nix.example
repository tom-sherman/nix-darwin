# Copy this file to local.nix and fill in your machine-specific values.
# local.nix is gitignored so it won't be committed to the repository.
{
  # The hostname of this machine (used as the darwinConfigurations key).
  # Run `hostname` or `scutil --get LocalHostName` to find your machine's hostname.
  hostname = "MBP-H53F7P3VNY";

  # Your macOS username (the short name shown in System Settings > Users & Groups).
  username = "Tom.Sherman";
}
